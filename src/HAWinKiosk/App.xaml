<Application x:Class="HAWinKiosk.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="Application_Startup">
    <Application.Resources>
        <FontFamily x:Key="RalewayFamily">pack://application:,,,/Fonts/Raleway-VariableFont_wght.ttf#Raleway, Segoe UI</FontFamily>
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="{StaticResource RalewayFamily}"/>
        </Style>
        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="FontFamily" Value="{StaticResource RalewayFamily}"/>
        </Style>
        <!-- Slight rounding: 6px -->
        <CornerRadius x:Key="UiCornerRadius">6</CornerRadius>
        <SolidColorBrush x:Key="Theme.Settings.Fg" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="Theme.Settings.FgMuted" Color="#444444"/>
        <SolidColorBrush x:Key="Theme.Settings.InputBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Theme.Settings.InputBorder" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="Theme.Button.SecondaryBg" Color="#F6F8FB"/>
        <SolidColorBrush x:Key="Theme.Button.SecondaryFg" Color="#1F2937"/>
        <SolidColorBrush x:Key="Theme.Button.SecondaryBorder" Color="#C8D2DE"/>
        <SolidColorBrush x:Key="Theme.Toggle.TrackOff" Color="#D0D8E4"/>
        <SolidColorBrush x:Key="Theme.Toggle.ThumbOff" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Theme.Toggle.TrackOn" Color="#16B9F0"/>
        <SolidColorBrush x:Key="Theme.Toggle.ThumbOn" Color="#B9E9FF"/>
        <SolidColorBrush x:Key="Theme.Toggle.DisabledTrack" Color="#B8C2D0"/>
        <SolidColorBrush x:Key="Theme.Toggle.DisabledThumb" Color="#E5E8EE"/>

        <!-- Settings (gear) — Segoe MDL2 Assets, not emoji -->
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#16B9F0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Cursor="Hand">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <!-- Padding=0: WPF also applies Control.Padding to the text; Border alone adds inset (avoid double left gap). -->
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Settings.InputBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Settings.Fg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Settings.InputBorder}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Theme.Settings.Fg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1.4"
                                CornerRadius="6"
                                Height="{TemplateBinding Height}"
                                Padding="10,0">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Padding="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox aligned with RoundedTextBox: white field, #CCC border, rounded -->
        <Style x:Key="SettingsComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Settings.InputBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Settings.InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Settings.Fg}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="{DynamicResource Theme.Settings.InputBg}"/>
                        <Setter Property="Foreground" Value="{DynamicResource Theme.Settings.Fg}"/>
                        <Setter Property="Padding" Value="10,8"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <ToggleButton x:Name="DropDownToggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          VerticalAlignment="Stretch"
                                          HorizontalAlignment="Stretch"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ToggleBorder"
                                                Background="{DynamicResource Theme.Settings.InputBg}"
                                                BorderBrush="{DynamicResource Theme.Settings.InputBorder}"
                                                BorderThickness="1.4"
                                                CornerRadius="6"
                                                Height="40"
                                                SnapsToDevicePixels="True">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="28"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter x:Name="ContentSite"
                                                                  Margin="10,0,4,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  IsHitTestVisible="False"/>
                                                <Path Grid.Column="1"
                                                      Data="M0,0 L4,4 L8,0 Z"
                                                      Fill="{DynamicResource Theme.Settings.FgMuted}"
                                                      Width="10"
                                                      Height="6"
                                                      Stretch="Fill"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource Theme.Settings.InputBg}"
                                        BorderBrush="{DynamicResource Theme.Settings.InputBorder}"
                                        BorderThickness="1.4"
                                        CornerRadius="6"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer Margin="0,4"
                                                  SnapsToDevicePixels="True"
                                                  HorizontalScrollBarVisibility="Auto"
                                                  VerticalScrollBarVisibility="Auto"
                                                  CanContentScroll="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="DropDownToggle" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Settings.InputBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Settings.Fg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Settings.InputBorder}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Theme.Settings.Fg}"/>
            <Setter Property="PasswordChar" Value="*"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1.4"
                                CornerRadius="6"
                                Height="{TemplateBinding Height}"
                                Padding="10,0">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Padding="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="#16B9F0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="22,14"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Cursor="Hand"
                                Padding="{TemplateBinding Padding}"
                                MinHeight="{TemplateBinding MinHeight}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,1,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pill toggle (auto-start), HA WinKiosk palette -->
        <Style x:Key="SettingsToggleSwitch" TargetType="ToggleButton">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid SnapsToDevicePixels="True" Width="50" Height="24">
                            <Border x:Name="Track"
                                    Height="22"
                                    CornerRadius="11"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource Theme.Toggle.TrackOff}"/>
                            <Border x:Name="Thumb"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="9"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"
                                    Margin="3,0,0,0"
                                    Background="{DynamicResource Theme.Toggle.ThumbOff}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.22" Direction="270"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource Theme.Toggle.TrackOn}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                                <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource Theme.Toggle.ThumbOn}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Left"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="3,0,0,0"/>
                                <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource Theme.Toggle.ThumbOff}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource Theme.Toggle.DisabledTrack}"/>
                                <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource Theme.Toggle.DisabledThumb}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Theme.Button.SecondaryBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Button.SecondaryFg}"/>
            <Setter Property="Padding" Value="22,14"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Button.SecondaryBorder}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Cursor="Hand"
                                Padding="{TemplateBinding Padding}"
                                MinHeight="{TemplateBinding MinHeight}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,1,0,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
